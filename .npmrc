# react-native-tvos publishes as prerelease versions (0.86.2-0), and a
# prerelease never satisfies a plain semver range, so every library declaring
# `peer react-native >=0.65 <1.0` rejects it. This is the fork's documented
# install mode, not a workaround for a real incompatibility.
legacy-peer-deps=true
